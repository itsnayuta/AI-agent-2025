def execute_schedule(schedule_info):
    return f"Đã thực hiện lập lịch: {schedule_info}"

def notify_schedule_change(user_email, change_info):
    return f"Đã gửi email tới {user_email} về thay đổi: {change_info}"
